function varargout = keepstr(col,str,varargin)%KEEPSTR keeps rows that contain the specified string in the specified column%    usage: [aa, [bb], [cc], ...] = kill(col, str, a, [b], [c], ...);%%	col = column to test%	str = string value to test for; keeps rows with string identical to this%	a, [b], [c], ... = one or more single column vectors, numeric or strings (data must be in columns)%%	returns: [aa, [bb], [cc], ...] = one or more single column vectors with rows kept%    %	StatToolbox. See also KEEP, KILL, KILLSTR.ncols = nargin-2; % Omit first three input values, col, min, maxif ncols < 1	error('Must supply at least one column of data.');endif col > ncols	error('Column specified to test is larger than number of input columns.');endif ~ischar(varargin{col})	error('Column to be tested contains numeric data.');endndx = strmatch(str, varargin{col}, 'exact');j=0;for i=1:ncols	j = j+1;	varargout{j} = varargin{j}(ndx,:);end