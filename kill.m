function varargout = kill(col,min,max,varargin)%KILL deletes rows that contain the range of values in the specified column%    usage: [aa, [bb], [cc], ...] = kill(col, min, max, a, [b], [c], ...);%%	col = column to test%	min, max = min and max values to test for; deletes rows with values equal to or between min and max%	a, [b], [c], ... = one or more single column vectors, numeric or strings (data must be in columns)%%	returns: [aa, [bb], [cc], ...] = one or more single column vectors with rows deleted%    %	StatToolbox. See also KILLSTR, KEEP, KEEPSTR.ncols = nargin-3; % Omit first three input values, col, min, maxif ncols < 1	error('Must supply at least one column of data.');endif col > ncols	error('Column specified to test is larger than number of input columns.');endif min > max	error('Doh! min must be less than or equal to max.');endif ischar(varargin{col})	error('Column to be tested contains non-numeric data.');endndx = find( ((varargin{col} < min) | (varargin{col} > max)) );j=0;for i=1:ncols	j = j+1;	varargout{j} = varargin{j}(ndx,:);end