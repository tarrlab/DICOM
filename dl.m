function [H, FA, dl, Cl] = dl(hits, signal_n, fas, noise_n)%dL computes sensitivity and bias using a signal detection model with logistic distributions.%	usage: [hits n_signal fa's n_noise]%	output: [corrected_hits corrected_fa's dl Cl]%%	Reference:%	Snodgrass, J. G. & Corwin, J. (1988) JEP:GEN, Vol. 117, No. 1, 34-50if nargin ~= 4	error('Usage: dl(hits, n_signal, fas, n_noise]');endH = (hits + 0.5) / (signal_n + 1.0);FA = (fas + 0.5) / (noise_n + 1.0);dl = log( (H * (1.0 - FA)) / (FA * (1.0 - H)) );Cl = 0.5 * log( (1.0 - FA) * (1.0 - H) / (H * FA) );temp = sprintf('Hits: %4.3f, FAs: %4.3f, dl: %4.3f, Cl: %4.3f', H, FA, dl, Cl);disp(temp);