function head(in_fname,varargin)%HEAD lists the first n lines of a text file%    usage:	head(in_fname, [n])%			in_fname = text file to list%			[n] = number of lines to list, if omitted n = 5%%	StatToolbox.% Find out number of rows in fileif nargin > 1	n = varargin{1};else	n = 5;endlines = 0;x = 0;% Open Data Filedatafile = fopen(in_fname, 'rt');% Loop through data file until we get a -1 indicating EOFwhile(x~=(-1) & lines < n)	x=fgetl(datafile);	disp(x);	lines = lines + 1;endlines = lines - 1;fclose (datafile);